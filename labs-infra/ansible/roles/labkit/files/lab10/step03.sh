#!/usr/bin/env bash
# Extracted from theory.md — Lab 10, command block 3.
# Read before running; some blocks expect variables set by earlier blocks.

k exec -it -n hk-lab10 observed -- sh -c 'id; printf "hk-lab10-marker\n"'
