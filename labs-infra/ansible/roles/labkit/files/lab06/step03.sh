#!/usr/bin/env bash
# Extracted from theory.md — Lab 6, command block 3.
# Read before running; some blocks expect variables set by earlier blocks.

docker image rm hk-study-local:current hk-study-local:v1
unset V1_ID V2_ID
