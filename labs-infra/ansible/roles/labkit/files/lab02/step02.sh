#!/usr/bin/env bash
# Extracted from theory.md — Lab 2, command block 2.
# Read before running; some blocks expect variables set by earlier blocks.

k create -n hk-lab02 --dry-run=server -f 02-valid.yaml
